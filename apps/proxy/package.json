{
  "name": "proxy-manager",
  "version": "1.0.0",
  "description": "Control-plane for PacketStream proxies: builds, stores and distributes proxy configs to the panel's apps. Traffic never flows through the panel (TZ §1).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "https-proxy-agent": "^7.0.6",
    "socks-proxy-agent": "^8.0.5"
  }
}
